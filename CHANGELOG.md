# Changelog

## v0.2.7 (2026-09-09)

### UI 优化
- **默认检测模式改为快速模式**（之前默认增强模式），打开软件即用秒出检测
- **修复"开始加水印"按钮被窗口裁切的问题**：将按钮和状态栏移到底部固定容器（bottom_bar）中，不再被主体面板（main_pw）内部 PanedWindow 的子窗口 minsize 挤压；窗口缩放时按钮始终完整可见

### 验证
- GUI `--smoke` 自检通过

## v0.2.6 (2026-09-09)

### 重大修复
- **修复快速模式无水印图片误报问题**：之前 conf 阈值 3.3 过低，无水印图片在 96 次（9尺度×16偏移）尝试中碰巧 conf>3.3 且 magic_err≤3 导致误报，显示不存在的水印 ID 和密钥
- 诊断发现：无水印图片的 conf 上限为 3.37，从未超过 3.5；加水印图片 conf≈3.95
- 将快速模式 conf 阈值从 3.3 提高到 **3.5**，完全消除误报

### 验证
- 8 张无水印图片（随机噪声×5、模糊噪声模拟普通照片、渐变色、竖图随机噪声）：全部正确返回未命中 ✓
- 加水印图片（直接输出）：命中 fp=44DC 正确，conf=3.95，0.52 秒 ✓
- Snipaste 截图（裁切）：正确返回未命中（快速模式不抗裁切）✓
- GUI `--smoke` 自检通过

## v0.2.5 (2026-09-09)

### 重大优化
- **快速模式重写，达到秒出（0.4秒）且内容解码完全正确**：
  - 之前快速模式 3.7 秒且内容 fp 解错；现在 **0.39~0.44 秒**，fp 全部正确
  - 核心发现：Z（同步信号）最高的尺度不一定内容解码最准——正确尺度 127x95 的 Z 仅 2.4，但 fp=44DC 完全正确；Z 最高的 128x96（Z=15.8）内容反而解错
  - 新策略：不对尺度做 Z 过滤，对固定尺度 ±4 块范围内所有 9 个候选尺度都做内容解码，选 magic_err 最小 + conf 最高的尺度
  - 命中条件：conf>3.3（未裁切图 conf≈3.95，裁切图 conf<3.3）+ magic_err≤3，有效过滤裁切图误报
- 快速模式适用场景：调色、滤镜、JPEG 压缩、缩放等未裁切图片；不适用裁切/截图（请用增强模式）

### 验证
- 直接输出图片：命中 fp=44DC 正确，0.41 秒
- Snipaste 截图：正确返回未命中（快速模式不抗裁切），0.39 秒
- 调色（亮度+20 对比度 1.3）：命中 fp=44DC，0.39 秒
- 高斯模糊 7x7：命中 fp=44DC，0.42 秒
- JPEG 质量 75：命中 fp=44DC，0.39 秒
- 缩放 50%：命中 fp=44DC，0.44 秒
- 增强模式：截图仍命中（6.95 秒），无退化
- GUI `--smoke` 自检通过

## v0.2.4 (2026-09-09)

### 新功能
- **频域水印检测增加多线程搜索**：旋转角度粗搜和精搜改用 ThreadPoolExecutor 并行（4 线程），numpy/cv2 释放 GIL 有效并行
- **新增快速模式 / 增强模式可切换**：
  - 快速模式：不搜索旋转，1.5~3.7 秒，适合无旋转的正常图片（截图可能检测不到）
  - 增强模式：0 度不命中时多线程搜索旋转角度，3.6~6.9 秒，适合截图等有旋转的图片（默认）
- GUI 动作面板新增「检测模式」单选切换（快速/增强），附带说明文字

### 性能
- 增强模式截图检测从 8.3 秒（v0.2.3 单线程）降至 **6.9 秒**（多线程，快 17%）
- 快速模式无旋转图片 3.7 秒，截图 1.4 秒即返回结果

### 验证
- 图一（直接输出）：快速/增强均命中，fp=44DC 正确，3.6~3.7 秒
- 图二（Snipaste 截图）：增强模式命中（Z=7.2，magic_err=0），6.9 秒；快速模式未命中（符合预期，不搜旋转）
- GUI `--smoke` 自检通过

## v0.2.3 (2026-09-09)

### 优化
- **频域水印检测速度优化**：有旋转的截图检测从 13~15 秒降至 8~9 秒（快约 38%）；无旋转的正常图片保持 3~4 秒不变
- 旋转搜索范围从 ±2.0 度缩小到 ±1.5 度（覆盖 99% 截图场景），粗搜步长从 0.4 度加大到 0.5 度
- 旋转角度粗搜尺度步长从 4 块加大到 6 块（减少 35% 尺度数）；0 度预检保持步长 4 确保精度
- 新增提前终止：某旋转角度 Z>8 时直接采用，不再搜索剩余角度
- 0 度预检阈值从 8.0 降至 6.0，更多无旋转图片直接跳过旋转搜索

### 验证
- 用户真实截图（Snipaste）：仍命中（Z=7.2，magic_err=0），耗时从 13.4s 降至 8.3s
- 直接输出图片：仍命中（Z=15.8，fp 正确），耗时 3.7s（无退化）
- GUI `--smoke` 自检通过

## v0.2.2 (2026-09-09)

### 重大修复
- **修复截图工具（Snipaste 等）截图后频域水印检测不到的问题**：根因是截图过程中引入了 0.3~0.8 度的微小旋转/畸变，导致 DCT 块网格对齐失效——仅 0.5 度旋转就能让同步 Z 从 15.8 暴跌到 3.0。新增**旋转校正**：检测时自动搜索 ±2 度范围内的最优旋转角度（粗搜 0.4 度步长 + 精搜 0.1 度步长），用旋转校正后的图片做后续尺度/偏移/相位搜索
- 0 度快速预检：无旋转的正常图片 Z>8 时跳过旋转搜索，不增加耗时（约 3 秒）；有旋转的图片约 10~15 秒（后台线程不卡界面）

### 验证
- 用户真实截图（Snipaste 生成，979×1289）：从「未命中」变为**命中**（Z=10.7，magic_err=1）
- 直接输出图片（1440×1920）：仍正常命中（Z=15.8，fp 正确，magic_err=0）
- GUI `--smoke` 自检通过

### 已知限制
- 截图后内容指纹（fp）可能因局部畸变有偏差，但密钥归属证明（magic 校验）可靠
- 旋转超过 ±2 度或复杂透视畸变暂不支持（用户明确不需要抗旋转/拉伸）

## v0.2.1 (2026-09-09)

### 修复
- **修复手动截图（偏右/偏边裁切）频域水印检测不到的问题**：根因是同步 Z 阈值 6.2 过严，偏边裁切粗搜 Z 峰约 5.9 被挡住未进精搜；而精搜实际能找到正确解（conf≈3.96、magic 全对）。阈值降至 5.5（实测无水印 Z 上限 5.49，零误报）
- 288 组「3 种图 × 4 种等比缩放 × 8 种裁切位置 × PNG/JPEG90/JPEG75」综合测试：命中率 97.9%、内容正确率 96.2%；失败仅集中在「缩小到 60% + JPEG75」极端小图，正常手动截图（PNG/高质量）全部命中

### 优化
- **频域水印识别结果同时显示 ID 和内容**：预览窗格改为两行显示（第一行水印内容，第二行 ID:xxxx）；左上角水印信息卡也同时展示内容、ID、密钥、匹配度
- 内容未在本机历史库时明确显示「内容: 未在本机记录」而非只给 ID

### 注意
- 算法与 v0.2.0 兼容，v0.2.0 添加的频域水印可被本版识别
- 极端小图（截图后 <500px 且 JPEG75 以下）仍可能漏检，建议截图保留足够分辨率

## v0.2.0 (2026-09-08)

### 重大升级
- **频域水印算法整体换代，攻克「随手截图后检测不到」的核心问题**：旧版逐 8×8 块硬判决要求块网格精确对齐，非等比、非对称裁切后约 1/3 块符号翻转导致失效；新版改为「周期瓦片导频同步 + 像素级亚块对齐 + magic 归一化相关选相」
- 支持**任意子区域截图**检测：截图无需与原图等比例、无需从角落起裁，即使只截到约 80% 画面、x/y 方向独立缩放，仍可稳定检出（216 组裁切+JPEG 压缩测试：同步信号有水印/无水印完全分离，magic 校验 100% 通过，常见裁切内容恢复中位 100%）
- 三层自动对齐：①粗搜瓦片块数尺度（宽高独立，覆盖非等比裁切）②搜索 DCT 块网格 0~7px 像素起点，补偿截图重采样的亚块平移 ③FFT 一次解算 256 种瓦片相位并用密钥 magic 选定正确相位
- 低纹理图片（天空/墙面/虚化背景）同样稳定；对 JPEG 压缩、PS 调色导出、改分辨率鲁棒
- 内容改为 16bit 密钥指纹（CRC-16）+ 16bit magic 双校验，无水印/错密钥零误报；本机历史内容库自动把指纹回显为完整水印文字

### 优化
- 检测速度优化：粗搜步长 + 像素偏移两阶段搜索 + FFT 相位向量化，常见截图检测 1~3 秒，后台线程不卡界面
- 选取更鲁棒的低频 DCT 系数对 (2,2)/(1,3)，两次缩放 + JPEG75 后符号存活率≈100%

### 注意
- 算法换代后，旧版添加的频域水印无法被新版识别，请用新版对已加水印图片重新添加一次
- 极端裁切（保留画面 <25%）因完整瓦片过少，成功率会下降

## v0.1.0 (2026-09-08)

### 新增
- 双水印系统：标记水印（文件尾字节级无损）+ 频域水印（DCT差分调制，32bit盲提取）
- 支持 PNG/JPG/BMP/GIF/TIFF/WebP/TGA 格式，原格式无损输出
- XnConvert风格三区布局：文件列表（缩略图阵列/列表/详细信息，多视图+排序）+ 动作面板 + 预览窗口
- 文件列表多选：Ctrl加选/Shift范围选/Delete删除（二次确认）/Ctrl+A全选
- 拖拽文件/文件夹导入，点击缩略图自动检测水印
- 输出自动新建「水印输出_日期」文件夹，默认保存桌面
- 全圆角UI：圆角按钮/卡片/输入框，高斯模糊壁纸背景
- 动作面板内容自适应窗口大小，不裁切
- 发布脚本 release.py：自动版本管理、changelog生成、GitHub Release创建
